@host = http://localhost:8080
@contentType = application/json

### Start demo-process instance
POST {{host}}/api/process-instances
Content-Type: {{contentType}}

{
  "processId": "demo-process",
  "variables": {
    "amount": 850,
    "customerId": 42
  }
}
